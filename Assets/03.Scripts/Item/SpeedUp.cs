using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpeedUp : MonoBehaviour

/// <summary>
/// 캐릭터컨트롤러의 MoveSpeed를 증가시키는 아이템
/// 바나나를 먹으면 30초 동안 스피드업 uiinventory에서 onusebutton 함수 case3
/// 코루틴으로 30초 후에 원래 속도로 돌아옴
/// 증가시킬 속도는 바나나 아이템데이터에서  설정
/// 
/// 
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
